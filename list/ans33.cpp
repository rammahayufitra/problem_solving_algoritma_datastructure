#include <iostream>
#include <vector>





int main(){
    std::vector<int> nums =  {10, 20, 30, 40};
    std::vector<int> generate_combination(nums){
        void backtrack(start,path){
            result.push_back(path);
            for(int i = 0; i<len(nums); i++){
                
            }

        }
        std::vector<int> result;
        backtrack(0, []);
        return result
    }
}